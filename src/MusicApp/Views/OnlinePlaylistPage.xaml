<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MusicApp.Views.OnlinePlaylistPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:MusicApp.Core.Models">

    <Grid>
        <TextBlock Opacity="0.7" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Text="{x:Bind ViewModel.Message, Mode=OneWay}"
                   Visibility="{x:Bind ViewModel.Message, Mode=OneWay, Converter={StaticResource StringToVisibility}}" />

        <ProgressRing Width="32" Height="32" HorizontalAlignment="Center" VerticalAlignment="Center"
                      IsHitTestVisible="False"
                      IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                      Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibility}}" />

        <ScrollViewer Padding="24,16,24,0"
                      Visibility="{x:Bind ViewModel.IsLoaded, Mode=OneWay, Converter={StaticResource BoolToVisibility}}">
            <StackPanel Spacing="20" Margin="0,0,0,110">

                <Grid ColumnSpacing="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Width="200" Height="200" CornerRadius="8"
                            VerticalAlignment="Top"
                            Background="{ThemeResource ControlAltFillColorSecondaryBrush}">
                        <Image Stretch="UniformToFill"
                               Source="{x:Bind ViewModel.Playlist.ThumbnailUrl, Mode=OneWay, Converter={StaticResource DecodedImage}, ConverterParameter=400}" />
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Bottom" Spacing="6">
                        <TextBlock Text="{x:Bind ViewModel.Playlist.Title, Mode=OneWay}"
                                   TextWrapping="Wrap" MaxLines="3" TextTrimming="CharacterEllipsis"
                                   Style="{StaticResource TitleLargeTextBlockStyle}" />

                        <TextBlock Text="{x:Bind ViewModel.Playlist.Author, Mode=OneWay}"
                                   Style="{StaticResource BodyStrongTextBlockStyle}" />

                        <TextBlock Text="{x:Bind ViewModel.CountText, Mode=OneWay}"
                                   Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}" />

                        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,10,0,0">
                            <Button Click="Play_Click"
                                    Style="{StaticResource AccentButtonStyle}"
                                    Visibility="{x:Bind ViewModel.HasTracks, Mode=OneWay, Converter={StaticResource BoolToVisibility}}">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon FontSize="14" Glyph="&#xF5B0;" />
                                    <TextBlock Text="Play" />
                                </StackPanel>
                            </Button>

                            <Button Click="Shuffle_Click"
                                    Visibility="{x:Bind ViewModel.HasTracks, Mode=OneWay, Converter={StaticResource BoolToVisibility}}">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon FontSize="14" Glyph="&#xE8B1;" />
                                    <TextBlock Text="Shuffle" />
                                </StackPanel>
                            </Button>

                            <Button Click="Save_Click"
                                    ToolTipService.ToolTip="Copy this playlist into your own playlists"
                                    Visibility="{x:Bind ViewModel.HasTracks, Mode=OneWay, Converter={StaticResource BoolToVisibility}}">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon FontSize="14" Glyph="&#xE710;" />
                                    <TextBlock Text="Save" />
                                </StackPanel>
                            </Button>
                        </StackPanel>

                        <TextBlock Text="{x:Bind ViewModel.SaveMessage, Mode=OneWay}"
                                   Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}"
                                   Visibility="{x:Bind ViewModel.SaveMessage, Mode=OneWay, Converter={StaticResource StringToVisibility}}" />
                    </StackPanel>
                </Grid>

                <ListView ItemsSource="{x:Bind ViewModel.Playlist.Tracks, Mode=OneWay}"
                          IsItemClickEnabled="True" ItemClick="Tracks_ItemClick"
                          SelectionMode="None" Padding="0"
                          ScrollViewer.VerticalScrollMode="Disabled"
                          ScrollViewer.VerticalScrollBarVisibility="Disabled">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:Track">
                            <Grid Height="60" ColumnSpacing="12" Background="Transparent"
                                  PointerEntered="Row_PointerEntered" PointerExited="Row_PointerExited">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="44" Height="44" CornerRadius="4"
                                        VerticalAlignment="Center"
                                        Background="{ThemeResource ControlAltFillColorSecondaryBrush}">
                                    <Image Stretch="UniformToFill"
                                           Source="{x:Bind ThumbnailUrl, Converter={StaticResource DecodedImage}, ConverterParameter=88}" />
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind Title}" TextTrimming="CharacterEllipsis" MaxLines="1"
                                               Style="{StaticResource BodyStrongTextBlockStyle}" />
                                    <TextBlock Text="{x:Bind SubtitleText}" TextTrimming="CharacterEllipsis" MaxLines="1"
                                               Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}" />
                                </StackPanel>
                                <TextBlock Grid.Column="2" VerticalAlignment="Center" Opacity="0.6"
                                           Text="{x:Bind DurationText}" Style="{StaticResource CaptionTextBlockStyle}" />
                                <Button x:Name="RowActions" Grid.Column="3" Click="RowActions_Click" Opacity="0"
                                        Width="32" Height="32" Padding="0" CornerRadius="6"
                                        VerticalAlignment="Center"
                                        Background="Transparent" BorderThickness="0"
                                        ToolTipService.ToolTip="More">
                                    <FontIcon Glyph="&#xE712;" FontSize="15" />
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
